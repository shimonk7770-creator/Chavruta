# חברותא - מדריך בדיקות והכנה להגנה (QA Checklist)

> מסמך זה הוא רשימת בדיקות מעשית שיש לעבור עליה **פיזית, לפני ההגנה** - לא רק לקרוא, אלא לבצע כל שורה בפועל ולסמן וי. הוא מבוסס במדויק על סעיף 12 ("נקודות תורפה אפשריות בהגנה") ועל קריטריוני הקבלה (סעיף 13) במסמך ה-SRS. המטרה: שום דבר בהגנה לא יפתיע אתכם.

---

## 1. הרשאות אמיתיות (לא רק הסתרת כפתורים) - AC-002, AC-003

בדקו כל אחד מהתרחישים הבאים **דרך קריאת API ישירה** (למשל עם DevTools -> Console, או Postman), לא רק דרך הממשק:

- [ ] התחברו כמשתמש רגיל (member). נסו לשלוח בקשת `PUT /posts/<id>` על פוסט **של משתמש אחר** - צריך לקבל 403, לא 200.
- [ ] נסו `DELETE /groups/<id>` על קבוצה שאתם לא מנהלים - צריך 403.
- [ ] התחברו כמנהל קבוצה אחת. נסו לערוך/למחוק פוסט בקבוצה **אחרת** שאתם לא מנהלים בה - צריך להיחסם (403), גם אם אתם "מנהל" באופן כללי.
- [ ] נסו לגשת ל-`GET /learning/<id-של-משתמש-אחר>/edit` - צריך 403 (מעקב לימוד הוא אישי, גם אדמין לא אמור לערוך).
- [ ] נסו לגשת לדף מוגן (`/profile`, `/learning`, `/feed`) בלי להיות מחוברים - צריך redirect ל-`/login`.

## 2. שחזור סביבה נקייה - AC-008, NFR-014

בצעו את זה על **תיקייה נקייה לגמרי** (או מחשב אחר), לא על התיקייה שאתם עובדים בה:

- [ ] `git clone` מהריפו הציבורי
- [ ] `npm install` - עובר בלי שגיאות
- [ ] יצרתם `serviceAccountKey.json` חדש (או העתקתם את הקיים) בשורש הפרויקט
- [ ] יצרתם `.env` מ-`.env.example`
- [ ] `npm run seed` - רץ בלי שגיאות, מדפיס "נזרעו בהצלחה..."
- [ ] `npm run dev` - השרת עולה, `http://localhost:3000` עובד
- [ ] בדקתם שיש לכם **גיבוי נגיש** של `serviceAccountKey.json` (לא בגיט!) - בענן אישי/דיסק-און-קי - ליום ההגנה עצמו

## 3. גרפים חיים (D3) - AC-006

- [ ] פתחו את דשבורד הסטטיסטיקה, שימו לב למספרים הנוכחיים
- [ ] הוסיפו פוסט חדש / רישום לימוד חדש
- [ ] רעננו את הדשבורד - **ודאו שהמספר השתנה בפועל** (לא נתון קבוע בקוד)
- [ ] נסו את הדשבורד כשאין עדיין נתונים (משתמש חדש) - ודאו מצב ריק ידידותי, לא גרף שבור

## 4. קלט עוין (XSS, הזרקות) - AC-007

בכל אחד מהשדות הבאים, נסו להזין את המחרוזת `<script>alert(1)</script>` ווודאו שהיא **מוצגת כטקסט רגיל** ולא מופעלת בפועל:

- [ ] תוכן פוסט
- [ ] תוכן תגובה
- [ ] הודעת צ'אט
- [ ] ביוגרפיה בפרופיל
- [ ] שם קבוצה / תיאור קבוצה

בדקו גם:
- [ ] טופס עם כל השדות ריקים - הודעת שגיאה ידידותית, לא קריסה
- [ ] טקסט ארוך מאוד (הרבה יותר מהמקסימום המוגדר) - נחתך/נדחה בעדינות
- [ ] חיפוש עם תווים מיוחדים (`$`, `.`, `{`, `}`) - לא גורם לשגיאת שרת

## 5. מקרי קצה בהרשאות ובנתונים

- [ ] משתמש חדש שלא הצטרף לאף קבוצה - הפיד מציג הודעה מזמינה, לא מסך ריק/שבור
- [ ] קבוצה חדשה בלי פוסטים - כנ"ל
- [ ] מחיקת חשבון (soft delete) ואז ניסיון התחברות עם אותו מייל - צריך להיכשל
- [ ] מנהל קבוצה יחיד מוחק את החשבון שלו - בדקו מה קורה לקבוצה (BR-009 - ידוע כפער פתוח, ראו סעיף "פערים" למטה)

## 6. עברית ו-RTL בכל מקום

- [ ] בדקו את עמוד ה-React (`/study-room`) - הטקסט מיושר לימין, לא "נשבר" ל-LTR
- [ ] בדקו את הצ'אט - כיווניות נכונה גם בהודעות עם מספרים/אנגלית מעורבים
- [ ] בדקו בדפדפן Chrome **וגם** Edge (NFR-013)

## 7. סודות ב-Git - AC-009

- [ ] `git log --all -p | grep -i "serviceAccountKey\|private_key\|MONGODB_URI"` - לא אמור להחזיר כלום
- [ ] `.env` ו-`serviceAccountKey.json` לא מופיעים ב-`git ls-files`

---

## פערים ידועים (לדעת להסביר, לא "להיתפס" עליהם)

אלו החלטות/מגבלות **מודעות** - אם המרצה שואל, זו תשובה טובה: "זו החלטה מתועדת, לא משהו ששכחנו":

1. **BR-009** (העברת ניהול קבוצה אוטומטית כשהמנהל היחיד מוחק חשבון) - לא מומש עדיין; כרגע הקבוצה נשארת עם `managerId` שמצביע על משתמש לא-פעיל. תיקון אפשרי: פונקציה שמעבירה ניהול לחבר הוותיק ביותר.
2. **מקביליות** (סעיף 12.7) - מדיניות Last-Write-Wins, בלי נעילות. אם שני אנשים עורכים אותו פוסט בו-זמנית, האחרון שומר מנצח.
3. **חיפוש טקסט ב-Firestore** (סעיף 12.11) - מבוצע בזיכרון השרת ולא באינדקס DB-native, כי Firestore לא תומך בכך כמו Mongo. פתרון סביר לגודל פרויקט לימודי, לא יתאים לאלפי רשומות.

---

*מסמך זה משלים את סעיף 12 ב-SRS-Chavruta.md ואת TASKS.md. לעדכן אחרי כל שבוע עבודה.*
